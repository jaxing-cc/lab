server:
  port: 8081

# datasource
spring:
    datasource:
      url: jdbc:mysql://123.56.251.13:3306/lab?serverTimezone=UTC&useAffectedRows=true&characterEncoding=utf-8&serverTimezone=UTC
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
    jackson:
      date-format: yyyy-MM-dd HH:mm:ss
      time-zone: GMT+8

logging:
    level:
      com.learnhow.springboot.mybatis.dao: debug
pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql
